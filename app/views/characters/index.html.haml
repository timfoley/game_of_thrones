%h2 The Fine People of Westeros

%ul
  - @characters.each do |character|
    %li
      = link_to character.name, character_path(character)
      =' | '
      = link_to character.house.name, house_path(character.house)
